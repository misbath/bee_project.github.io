---
title: "Data Processing"
output: html_document
---

```{r setup, include=FALSE}
library(tidyverse)
library(viridis)
knitr::opts_chunk$set(
	echo = TRUE,
	warning = FALSE,
	fig.width = 8, 
  fig.height = 6,
  out.width = "90%"
)
options(
  ggplot2.continuous.colour = "viridis",
  ggplot2.continuous.fill = "viridis"
)
scale_colour_discrete = scale_colour_viridis_d
scale_fill_discrete = scale_fill_viridis_d
theme_set(theme_bw() + theme(legend.position = "bottom"))
```

```{r, county FIPS data}
county_fips = read_csv("./data/all-geocodes-v2016.csv", skip = 4) %>%
  janitor::clean_names() %>%
  rename(county_name = area_name_including_legal_statistical_area_description) %>%
  filter(county_code_fips != "000") %>%
  mutate(state_county_fips = paste0(state_code_fips, county_code_fips)) %>%
  select (state_code_fips, county_code_fips, state_county_fips, county_name)
```

```{r, county-level bee data}
bee_county = read_csv("./data/bee_county/Bee_Colony_Census_Data_by_County 2.csv") %>% 
  janitor::clean_names() %>% 
  select(year, 
         state, 
         state_ansi, 
         ag_district, 
         ag_district_code, 
         county, 
         county_ansi, 
         value, 
         cv_percent)

   
  
  
head(bee_county)
```

```{r, state-level bee data}
## bee_state_data contains unzipped files that contain all of the information gathered in a year

## bee_state_2 contains 1 table per file, unzipped, that has summary table of honey producing colonies

## NONE OF THE CODE BELOW IS CORRECT

# option 1
file_name <- list.files(path = "./data/bee_state_2/") 

import_data = function(x) {
  df = read_csv(file = str_c("./data/bee_state_2/", x)) %>%
  mutate(file = x)
  df
}

  df = read_csv(file = str_c("./data/bee_state_2/", file_name[1])) %>%
  mutate(file = file_name[1])
  df
  
  my_read_csv = function(x){
    df = read_csv(x, skip = 9,col_names  = F)
  }
  
  test_df = my_read_csv("./data/bee_state_2/hony_002_2002.csv")
  

bee_data = 
  tibble(
    file_names = file_name,
    path = str_c("./data/bee_state_2/", file_names)
  ) %>% 
  mutate(data = map(path, my_read_csv))%>% 
  unnest()


do.call(rbind.data.frame, bee_data)
#option 2, load all datasets into R individually
bee2002 = read_csv("./data/bee_state_2/hony_002_2002.csv", skip = 9)

bee2019 = read_csv("./data/bee_state_2/hony_all_tables_2019.csv", skip = 9)

bee_state_load_and_tidy = function(path, range, year) {
  df = read_csv(path, range = range) %>%
    janitor::clean_names() %>%
    df
}

#creates merged dataframe but this is not correct. data columns are all shifted and wrong
bee_state = list.files(path = "./data/bee_state_2/", pattern = "*.csv", full.names = TRUE) %>%
  map_df(~read_csv(., col_types = cols(.default = "c"))) 

data_bee_state = plyr::ldply(bee_state, read_csv)

#similar to option above
load_data <- function(path) { 
  files <- dir(path, pattern = '*.csv', full.names = TRUE)
  tables <- lapply(files, read_csv)
  bind_rows(rbind, tables)
}

bee_state <- load_data("bee_state_data")

```



